import React from "react";
import { useNavigate } from "react-router-dom";

const Program = () => {
  const navigate = useNavigate();

  return <div className="upz-page-program">Program</div>;
};

export default Program;
